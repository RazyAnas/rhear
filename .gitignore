*.zip
.DS_Store

# ESP-IDF build output and fetched dependencies. Regenerated by `idf.py build`
# and already the largest single contributor to this repo's size.
psram_test/build/
esp32_test/build/
psram_test/managed_components/
esp32_test/managed_components/
**/sdkconfig.old

# Python bytecode
__pycache__/
*.pyc

# Regenerable fixtures. demo/captures/_synthetic.npz and its wav are produced by
# a few lines in demo/README.md; the plot is kept because it is the slide.
demo/captures/*.npz
demo/captures/*.wav

# SKiDL scratch output
hardware/skidl_REPL.*
